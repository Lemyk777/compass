# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — never commit real secrets
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel / typescript
.vercel
*.tsbuildinfo

# raw logo drop (copied into public/logos)
/logos/

# personal, per-machine Claude permissions (each dev keeps their own)
.claude/settings.local.json
