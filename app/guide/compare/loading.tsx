import { GuideSkeleton } from "@/components/guide/Skeleton";

// No dynamic child under compare either — see the note in
// app/guide/(index)/loading.tsx.
export default function GuideCompareLoading() {
  return <GuideSkeleton />;
}
